# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/__pycache__/
*.pyc
.secrets/
accounts.env

# Z.ai skills — not needed for the bot
skills/


# === SECURITY ===
.env
.env.*
.secrets/
*.env
accounts.env
*_session_string*
*.session
upload/
# [PR-7] runtime data stores — defence-in-depth even if DATA_DIR is overridden
sessions/
logs/
secrets/
credentials/
*.session-journal
*.session-shm
# never commit StrippedSession / JSON session exports
*.session.json
session_*.json

# === Test/CI infrastructure (Task 8a) ===
.venv-test/
.venv*/
__pycache__/
*.pyc
# Local SQLite DBs created at runtime (DATA_DIR defaults to ./data)
/data/
*.db-journal
*.db-wal
*.db
# Render/blueprint local snapshots
render.yaml.local
dash-after-sso-fix.png

# AI test harness secrets (Render env keys) — NEVER commit
.test_env.json
*.log
test_real_requests_results.json
