# Core configuration
BOT_TOKEN=
ADMIN_IDS=
CHANNEL_ID=

# Optional configuration
DATABASE_PATH=data/links.db
LOG_LEVEL=INFO
LOG_FORMAT=json

# Background jobs
VALIDATION_INTERVAL_HOURS=24
BACKUP_INTERVAL_HOURS=24
BACKUP_RETENTION_DAYS=30

# Rate limiting
RATE_LIMIT_PER_MINUTE=10

# HTTP server
HTTP_PORT=10000
HTTP_HOST=0.0.0.0

# Telegram API (optional, for userbot features)
API_ID=
API_HASH=

# Plugin configuration (comma-separated module paths)
PLUGINS=
